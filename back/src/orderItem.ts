export class OrderItem {
    private id:number;
    private quantity: number;
    private id_product: number;

    constructor(id:number, quantity: number, id_product: number) {
      this.id = id;
      this.quantity = id;
      this.id_product = id_product;
    }
}
